# YAML configuraton file for the Mustang Mach-E CANbus Simulator
mme:
    builtin:
        modules:    True
        pids:       True

    custom:
        modules:
            - module:
                name:               'ABC'
                channel:            'can0'
                arbitration_id:     0x700

 